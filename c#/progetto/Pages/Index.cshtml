@page
@model IndexModel
@{
    ViewData["Title"] = "Home page";
}

<div class="container mt-5">
    <!-- Sezione principale con titolo, descrizione e pulsante -->
    <div class="text-center py-5">
        <h1 class="display-4 text-primary mb-4">Welcome to the Bookcase</h1>
        <p class="lead text-secondary mb-5">Your personal library is just a click away. Explore and manage your books effortlessly.</p>
        <a asp-area="" asp-page="/SignUp" class="btn btn-lg btn-success px-4 py-2">Sign Up</a>
    </div>

    <!-- Riga per le colonne laterali (left e right) -->
    <div class="row">
        <!-- Colonna di sinistra (aside left) -->
        <aside class="col-md-6 mb-4">
            <div class="bg-light p-4 rounded description">
                <h4>Cedri Davide</h4>
                <p>Hi everyone, I'm Davide Cedri, I welcome you to our site. I am a book enthusiast, especially novels and thrillers, which I love. </p>
                <p>Here you will find the most iconic books written by many authors and you are spoiled for choice. I hope to convey passion in reading, have fun!</p>
            </div>
        </aside>

        <!-- Colonna di destra (aside right) -->
        <aside class="col-md-6 mb-4">
            <div class="bg-light p-4 rounded description">
                <h4>Parenti Michele</h4>
                <p>Greetings everyone, I'm Michele Parenti and I welcome you to our virtual space. I am a voracious reader, with a predilection for novels and thrillers, which always captivate me. </p>
                <p>Here you will find a selection of the most beautiful books by many authors, to satisfy every taste. I hope to infect you with my passion for reading, happy discovery! </p>
                </div>
        </aside>
    </div>

    <style>
        .signup-link {
            font-size: 1.2rem;
            font-weight: bold;
            color: #fff;
            text-decoration: none;
            border: 2px solid #28a745;
            border-radius: 5px;
            padding: 10px 20px;
            transition: background-color 0.3s, color 0.3s;
        }

            .signup-link:hover {
                background-color: #28a745;
                color: white;
            }

        .text-center {
            margin-top: 50px;
        }

        /* Aggiungi ombra per migliorare la visibilità delle sidebar */
        aside .bg-light {
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }

        /* Effetto hover per i paragrafi di descrizione */

        .signup-link {
            font-size: 1.2rem;
            font-weight: bold;
            color: #fff;
            text-decoration: none;
            border: 2px solid #28a745;
            border-radius: 5px;
            padding: 10px 20px;
            transition: background-color 0.3s, color 0.3s;
        }

            .signup-link:hover {
                background-color: #28a745;
                color: white;
            }

        .text-center {
            margin-top: 50px;
        }

        /* Aggiungi ombra per migliorare la visibilità delle sidebar */
        aside .bg-light {
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }

        /* Effetto hover per i paragrafi di descrizione */
        .description:hover {
            transform: scale(1.05);
            background-color: rgba(108, 170, 90, 1); /* Corretto il valore alpha */
            transition: transform 0.3s, background-color 0.3s;
        }

        aside .bg-light:hover {
            box-shadow: 0 0 10px rgba(108, 170, 90, 1);
        }
    </style>
</div>